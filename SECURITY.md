# Security policy

Report vulnerabilities through GitHub private vulnerability reporting. Never
attach provider credentials, private feed URLs, schedules, SageTV appdata,
`Sage.properties`, `Wiz.bin`, or logs to a public issue.

Security fixes must retain XXE/external-entity containment, download and image
limits, URL/command redaction, atomic writes, and fail-safe guide updates.
